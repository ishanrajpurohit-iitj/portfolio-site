# build output
dist/
.astro/

# dependencies
node_modules/

# environment variables
.env
.env.production

# macOS
.DS_Store

# logs
npm-debug.log*

# local Claude Code settings (workspace-specific, not shared)
.claude/

# raw source footage (384MB) — already compressed/trimmed into the actual
# assets used on the site (public/images/work/photoshop2030/*.mp4); this
# folder is just the original uploads, never referenced by any page
public/images/work/photoshop2030/adobe case study videos/
